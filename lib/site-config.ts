/**
 * CLIENT-SAFE site configuration entry point.
 * This file NO LONGER imports from ./supabase/server to prevent build errors.
 * Server-side components should import from ./site-config-server instead.
 */

export * from "./config-defaults";
